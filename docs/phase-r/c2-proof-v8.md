# 定理 C2 v8 — 窓短縮+相対速度 Minkowski 控除(審査サイクル10付議版)

> v7(サイクル9)の残項目5点への修復。中核は **T_a := T_s/16 の窓短縮**:
> 控除体積(窓長の2乗)を ↓256倍、μ は線形減(C₀ に吸収)、α掃引も縮小して
> 幾何余裕が30倍化 — 一手で項目1・3・4が同時に楽になる。

## 定理(v8)

ν 等方かつ**殻 S 上で Lebesgue に関し有界密度**(密度上界 Λ_ν; ガウス等の物理的 ν は
自明に満たす。純等方のみでの成立は予想として分離)、殻質量 q > 0、R=1、
K_mix := w₁T_s ≥ **K₀ = 5**。C₀ = C₀(τ/T_s, Λ_ν) が存在し
m₁ = pρqπw₁T_s > C₀ ⇒ 完全モデルは正確率で生存。

## 修復1: 相対速度 Minkowski 控除補題(サイクル9 §1 の全面書き直し)

- 固定 v の前像は**相対速度方向 v−v_P の管**(v7 の「方向 v」は誤り — 訂正)。
  過去サイトとの交差条件は D(v) := Δ̃ − v·jT_g ∈ 𝒦(v)、
  𝒦 = B(0,2R) ⊕ seg((v−v_P)T_a) ⊕ seg(−(v−v_{P'})T_a')(サイクル9(b) の再構成を採用)
- **正直な体積**: vol(𝒦) ≤ 4R·L₁L₂·sinθ + π(2R)²(L₁+L₂) + (32π/3)R³、
  L ≤ 0.56w̄·T_a。**T_a = T_s/16 により L ≤ 0.035·w̄T_s = 0.035K_mix·R** —
  二次項が激減し、K₀=5・τ=0 の最悪で
  **Σ_j (2j+1)·vol(𝒦)/((jT_g)³(2h)³) = 0.187 < 1/2**(数値検算表を付録に;
  τ=1: 0.023、τ=3: 0.003 — τ/T_s に単調減少)
- **サイト数補題(サイクル9(d) の位置窓論法を明文化)**: 控除は探索済み**全**サイトに
  わたるが、ギャップ j のサイトが交差しうるには Δ̃ ∈ jT_g·(v窓) ⊕ 𝒦 が必要 —
  v窓の直径 ~ 2h√3 より位置窓の径 ≈ j·a√3。y トラック間隔 8a で割り
  **交差可能サイト数 ≤ 2j+1**(依存錐でなく位置窓から導出 — 発散懸念の解消)
- **ν-測度比較**: 控除 v-集合(薄スラブ)の ν̂-測度 ≤ Λ_ν·(Lebesgue 割合)·c —
  有界密度仮定で閉じる(純等方のみの場合は open として分離)
- ドリフトは D(v) の平行移動で測度不変(サイクル9 で正当と確認済み)—
  全履歴一様・決定論的

## 修復2: 検証器 v4(サイクル9 §2 の2点を実装)

- Lipschitz の |v_p| を**摂動込み sup**(+0.05w̄√3)に、t* = T_g 設計を明記
- T_a = T_s/16 で再実行: **K₀=5 で全 τ 最悪余裕 ≥ +0.0029w̄**(v3 の30倍 —
  罰則(~0.003w̄ 級)の保守性の緩みに対する頑健性を回復)

## 修復3: 正測度パッチ(サイクル9 §3)

窓短縮の副産物: slack ≥ 0.0029w̄ > I* 帯半幅 w̄/400 = 0.0025w̄ —
**ℓ∞ 管の動径足跡が I* 帯を全被覆**し、動径再鳩ノ巣は不要化。
ν(A ∩ I* ∩ D_±) ≥ (q/200)·(slack/4w₁)²·c がそのまま成立(角度側はサイクル9 承認済み)

## 修復4・5

- μ の再計算: T_a 短縮で μ ≥ c₄(τ/T_s)·m₁/16 — C₀ に 16 を吸収(注記)
- 費用項/h の (K_mix, τ/T_s) 非増加性: 各項の明示式(進入 ∝ 1/((1+τ/T_s)K)、
  掃引 ∝ T_a/(T_g−T_a)、丸め・I* は定数)から項別に証明(1段落を付録に)

## 状態

サイクル9 残項目: 1(補題書き直し)→ 相対速度 Minkowski 版+数値検算 /
2(サイト数)→ 位置窓補題 / 3(動径)→ slack 増で不要化 / 4(Lipschitz)→ v4 実装 /
5(単調性)→ 付録化。**ν 有界密度を定理仮定に追加**(唯一の主張変更 —
物理的応用では無害、純等方版は予想として分離)。審査サイクル10(完結判定)へ。
