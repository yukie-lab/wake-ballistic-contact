# Phase R 主張と証明(セッション1)

> 規約はチェックポイント §0(device と一致)。M(ρ, ν, R, p, τ, T_s)。
> deg = (4π/3)ρR³、λ = ρπR² E|v_rel|、m = p(deg + λT_s)。

## 定理 A(絶滅・第一モーメント)【状態: **改訂版**証明あり(再マーク禁止版)/ 再マーク版は補題ギャップ1件】

> **改訂(セッション2・自己審査による)**: 初版の「m<1 ⇒ 絶滅」は誤り。
> 経路の隣接ステップ因子が速度を共有して相関するため(|v_{j−1}−v_j| と |v_j−v_{j+1}|)、
> スカラー m^k への因数分解は不当。正しくは転送核のスペクトル条件(下記)。
> E1 の数値(閾値 m≈1.0–1.3)はガウス殻で相関補正が小さいことと整合し影響なし。
> **自己審査が実誤りを検出した記録**(検証文化の実績4件目)。

**主張(改訂)**: 転送核 K(v, v′) := p·ρ·(V_R + πR² |v−v′| T_s) を ν 上の
積分作用素 (Kf)(v) = ∫ K(v,v′) f(v′) ν(dv′) とみなし、そのスペクトル半径を r(K) とする。
再マーク禁止版の M において **r(K) < 1 ならば** E[総マーク数] < ∞、したがって a.s. 絶滅。
**系 A′(有界速度)**: 速さ ≤ w_max なら m̄ := p·ρ·(V_R + 2πR² w_max T_s) ≥ sup-ノルム界で、
m̄ < 1 ⇒ 絶滅。**注**: 平均場値 m = p(deg+λT_s) は r(K) の下界(Jensen)であり、
m < 1 は絶滅の十分条件では**ない**(初版の誤り)。真の絶滅域は m ≤ r(K) < 1。

**証明**:
種を原点の追加点とする(Palm)。マーク事象は親試行の木をなす。深さ k のマーク点は
「時間順序つき開路」(i_0=種, i_1, …, i_k)に対応する: 各ステップで
(a) 順序対コイン U ≤ p、(b) 時刻列 t_1 ≤ … ≤ t_k が存在して t_j ∈ T_{i_{j-1} i_j} かつ
t_j ≤ t_{j-1} + τ + L_{j-1}(L は iid Exp(T_s):親は自分のマーク t_{j-1}+τ から
寿命 L_{j-1} の間だけ試行できる)。これはマーク成立の必要条件(緩和)である。

鍵の補題(**カプセル体積・厳密**): 速度 w の点が、時刻区間 [s, s+L] に源(位置 x、速度 v)
の半径 R 球と接触窓を交わらせる初期位置の集合は、半径 R のカプセル
(球 + 掃引円筒)であり、その体積はちょうど V_R + πR²|v−w|L。
ゆえに源1つ・寿命 L あたりの「使用可能ペア」の期待数は
ρ E_L[V_R + πR² E_w|v−w| L] = deg + λ T_s (源の速度 v で平均すれば E|v_rel|)。

相異なる k 点組への multivariate Mecke 公式で、位置積分はステップごとにカプセル体積を
与え(寿命 L_j で Exp 平均: E[L]=T_s)、速度積分が残る:
E[#深さ k の開路] ≤ ∫ ν(dv₀) (K^k 𝟙)(v₀) ≤ C_ν · r(K)^k (1+o(1))
(K は上記転送核。隣接因子は v_j を共有するためスカラー化できず、作用素冪で評価する)。
r(K) < 1 なら Σ_k が収束し E[総マーク数] < ∞。
マーク数有限+寿命有限 ⇒ 生存集合はいつか空。∎
(細部: (K^k 𝟙) の増大が r(K) で押さえられるための ν の可積分条件 — ガウスは
全モーメント有限で K はコンパクト正作用素、Krein–Rutman で r(K) は主固有値。審査対象。)

**ギャップ(再マーク許可版)**: 経路が同一点を(非連続に)再訪しうる。順序対は
一度しか試行しないため辺の再利用はないが、再訪点のステップは Mecke の新鮮積分に
ならない。予想: 疎領域では補正は O(ρR³) で閾値を動かさない。
**未解決タスク A-1**: 再マーク版の第一モーメント補正 or 別法(質量輸送?)。
数値(A-1 実施済み 2026-08-15): 装置に再マーク計数を追加(テスト PASS 維持)。
疎領域 (ρR³=0.1, m≈1.95) の実測: 再マークは全マーク事象の **8.5%** — 再マーク補正が
摂動級であることの傍証(閾値近傍での定量化は次セッション)。

## 定理 B(静的極限の同定)【状態: 証明あり(短い)】

**主張**: ν = δ_0(全点静止)のとき、M の到達集合はボンド希釈 RGG G(ρ, R; p)
(Poisson 点、距離 ≤ R の対を独立に確率 p で開く)における種の連結成分と一致する。
特に生存(総マーク無限)⟺ 種の成分が無限 ⟺ G(ρ,R;p) がパーコレートし種がその成分に入る。
**T_s と τ は生存に無関係**。

**証明**: 静止では T_ij = [0,∞)(|x_ij| ≤ R のとき)または空。j がマークされた瞬間
m_j に近傍対は「窓内」なので a_ij = m_j で即時試行(死亡は m_j より後、試行は
撃ちっ放しなので寿命は介在しない)。帰納的に、マーク集合 = 開辺で種から到達可能な
集合、マーク時刻 = ホップ数 × τ。∎

**数値**(E2, ρR³=1.0, deg=4.19): 巨大クラスタ出現は p ≈ 0.50–0.55。
(平均場 1/deg=0.24 と素朴 2.7/deg=0.65 の間 — ボンド RGG の文献値/独立実装照合は
タスク B-1: 動力学なしの純グラフ・ユニオンファインドで独立照合する。)

## 補題 C(使用可能ペア数の速度単調性)【状態: 証明あり】

**主張**: ν → s·ν₀(速度の一様スケール)のとき、源1つあたりの使用可能ペア期待数
deg + λ(s)T_s は s に単調非減少。deg 項は s に依存しない(時空定常性)。

**証明**: λ(s) = ρπR² s·E|v_rel|₀ は s に線形。deg は配置の定常性のみによる。∎

**注意**: これは分枝平均の単調性であり、生存確率そのものの s 単調性(予想 C4)は
未証明。結合(coupling)の障害: s を上げると接触窓が短くなり、
「マーク前に始まった窓を後から使う」機会(β_ij ≥ m_i 条件)は失われうる。
ただし E3 の数値は劇的に単調(σ: 0→0.1→0.25 で生存率 0.12→0.75→1.00)。
**タスク C-1 の結果(2026-08-15)**: 反例探索を理論的急所2点で実施 —
(E4) 静的閾値近傍 p=0.55・T_s=1.5・τ=0.5: 単調(0.69→1.00)。
(E4b/E4c) 静的ぎりぎりパーコレーション ρR³=0.7・p=1・T_s=1・τ=1:
16実現で σ=0.03 に谷の兆候 → **64実現で棄却**(0.734→0.781→0.797→0.844→0.906、
きれいに単調)。**棄却済みの枝**: 「小σ臨界谷」仮説は装置により棄却。
C4 は数値的に強固 — 次の仕事は結合(coupling)による証明構成。

## 数値サマリ(E1–E3、2026-08-15)

- E1(疎 ρR³=0.1): 生存の立ち上がりは m ≈ 1.0–1.3(有限時間打ち切りの軟化込み)。
  定理 A の m<1 絶滅と整合、予想 C2(m>c₀≈1 で生存)を支持
- E2(静的): 転移 p≈0.5(定理 B の対象。値の照合はタスク B-1)
- E3(層3デモ, ρR³=1.0, p=0.5, T_s=3): 静的 12% → σ=0.1 で 75% → σ≥0.25 で 100%。
  **「星の運動は入植を助ける」の最初の定量的実証**(進入駆動・撃ちっ放し規約の下で)。
  静的 m=2.09 で死に、運動 m=3.2 で生きる = パーコレーション制御↔分枝制御の交叉(C5)

## 規約依存性の明示(層3の解釈に必須)

進入駆動・撃ちっ放し(交付文書 §1.1)だから運動が単調に効く見込み。
滞在時間要求型(伝播に接触継続 τ_hold が必要)なら高速側で非単調になるはず。
この対比自体を層3の定理の一部として定式化する(タスク D-1: 滞在時間型の
装置バリアントと閾値比較 — ただしスコープは交付文書の規約が主、対比は従)。


## 命題 D(層3の帰着)【状態: 条件付き証明あり — C2 に帰着】

**主張**: C2(∃c₀: 混合領域 K>K₀ で pλT_s > c₀ ⇒ 正確率生存)を仮定する。
このとき、任意の静的サブクリティカル密度 ρR³ < η_c(連続体パーコレーション臨界)・
任意の p ≤ 1・T_s < ∞ に対し: 静的過程は a.s. 絶滅(定理 B + RGG サブクリティカル)、
一方 s を十分大きく取れば M(s·ν₀) は正確率で生存(λ(s) ∝ s → pλT_s > c₀)。
**すなわち「静的幾何が禁じる領域で、運動が入植を可能にする」— 層3の中核言明は
C2 の証明に完全に帰着した。**∎(C2 条件付き)

## C4 結合構成の障害の同定と再定式化(セッション1)

- **時間再スケーリング同値**: survival(ρ, s, p, τ, T_s) = survival(ρ, cs, p, τ/c, T_s/c)。
  ゆえに C4(s 単調性)⟺ 固定速度での (τ, T_s) **同時膨張**の単調性。
- **pathwise 結合の障害**: 早期マーキングは対 (k,l) の試行に有利とは限らない
  (窓 [m_k, d_k] が α_kl 前に終わりうる)。接触過程の attractiveness の
  標準論法は「対ごと一度・進入時刻付き」の構造でそのまま通らない。
- 方針転換: 完全単調性より先に、**漸近版(命題 D)= C2 帰着**で層3を取りにいく。
  完全単調性 C4 は数値的に強固(E4/E4b/E4c)なので予想として保持。

## C2 証明戦略(次セッションの山 — スケッチ)

1. 速度切断 |v| ≤ v_cut(質量 1−ε 残し): 標的の間引きは生存の下界方向に安全
2. 時空ブロック化: ブロック内で「入口領域のマーク粒子が隣接ブロック入口に
   ≥2 のマークを渡す」good 事象。K = E|v_rel|T_s/R ≫ 1 の新鮮領域性
   (寿命内移動 σT_s ≫ R)で good 確率 → 1
3. 有界速度 ⇒ 依存は有限レンジ ⇒ Liggett–Schonmann–Stacey 優越で
   方向付きパーコレーションに帰着 ⇒ 生存
4. 書き下しの主な労働: 入口領域の粒子集合の独立性の管理(粒子は (x₀,v) 空間の
   Poisson 点 — 「複数ブロックに入る粒子」の二重使用の除去、pair-once の会計)


## C6 数値結果(E5、2026-08-15)【状態: 両分岐とも数値支持】

T_s=∞、ρR³=0.05、p=1、RMS 速さを一致させた2分布(6実現ずつ、箱端到達前で打ち切り):
- **ガウス(非有界)**: 前線成長率 後半/前半 = **1.34 ± 0.14** — 加速(超線形の兆候)。
  機構どおり: より速い粒子が順次マークされ前線を運ぶ(CN19 𝒱 の無限系版)
- **固定速さ(有界)**: **0.98 ± 0.01** — 線形前線(形状定理型)。
層2の定式化目標: 有界 ν では線形前線+形状定理、非有界では R(t)/t→∞ の証明。
有界版の下界は C2 のブロック構成から従う見込み。非有界版の証明はおそらく容易
(任意速度階級の粒子がいつかマークされる → 下から σ_w t に漸近)— 次セッションで書く。


---

# セッション2(2026-08-15)

## 定理 C6a(超線形前線)【状態: 証明あり(完全)】

**主張**: T_s = ∞、p > 0、ν の速さ分布の台が非有界(例: ガウス)なら、
マーク集合の原点からの最大距離 R(t) は a.s. R(t)/t → ∞。

**証明**: 任意の W > 0 を固定。速さ ∈ [W, W+1] の粒子クラスの強度は
ρ_W = ρ·ν(shell_W) > 0。種粒子 s₀ は永続的にマーク済み(T_s=∞)であり、
クラス W の粒子と率 λ_W = ρ_W πR² E[|v_{s₀} − w|] > 0 で新規遭遇する
(弾道ペアの遭遇は各ペア高々1回・ペアごと独立コイン p)。遭遇は s₀ の軌道に
沿ったカプセルへの Poisson 点の入射だから、成功伝播の総数は平均 p λ_W t の
Poisson 変数で t→∞ で a.s. 無限大。よってあるクラス W 粒子 j がいつか(時刻 t_W で)
マークされる。以後 |x_j(t)| = |x_j(t_W) + v_j (t−t_W)| ≥ W t (1−o(1))。
ゆえに liminf R(t)/t ≥ W。W は任意。∎

**注**: E5 の実測(ガウスで成長率比 1.34)はこの定理の有限時間の影。
CN19 の「前線は分布の裾の最速星が運ぶ」(𝒱 因子)の無限系版が定理になった。

## 命題 C6b(有界速度の線形上界)【状態: 証明あり(τ>0)+ 稠密 τ=0 の病理を記録】

**主張**: 速さ ≤ w_max(a.s.)、τ > 0 なら R(t) ≤ w_max t + R·(t/τ) + R = O(t)。
**証明**: マークは粒子上を運ばれ(移動 ≤ w_max t)、伝播1回の空間ジャンプは
進入時の距離 ≤ R。時刻 t までの連鎖のホップ数は各ホップが τ を消費するため ≤ t/τ。∎
**病理の記録**: τ = 0 かつ瞬間配置の RGG が超臨界(ρR³ > η_c 級)だと、
種の瞬間クラスタ全体が時刻 0 に一斉マークされ R(0⁺) = ∞ になりうる。
**τ > 0 が稠密領域の局所性を担保する**(規約の物理的意味: 探査機の旅行時間)。
疎領域(瞬間 RGG サブクリティカル)では τ=0 でも連鎖長の指数裾で線形性が保たれる。

## 定理 C2(生存 — セル・リレー構成)【状態: 証明スケッチ完成(厳密化可能な水準)/ 審査前】

**主張**: ν は等方で、ある速さ殻 [w₁, w₂](0<w₁<w₂<∞)に正の質量 q を持つとする。
定数 C = C(殻の幾何, τ/T_s) < ∞ が存在し、
**p · ρ πR² w₁ q² · T_s > C ならば正確率で生存する。**
(定理 A と合わせ、臨界面は2つの分枝型表式の間に挟まれる — **層1完結**。
命題 D と合わせ、**層3「十分速い運動は静的幾何が禁じる入植を可能にする」が定理になる**。)

**構成(証明の骨格)**:

*(0) 安全な間引き*: 粒子を速さ殻 [w₁,w₂] に間引く(過程は付加的単調 —
間引き版の生存 ⇒ 元の生存)。再マークも無視。以後 ν は殻上等方、強度 ρq。

*(1) 時空セル格子*: 世代時間 T_g := 2(τ + T_s)、箱幅 ℓ := w̄ T_g/2(w̄ = 殻平均速さ)。
サイト (n,k) ∈ ℤ×ℕ(n+k 偶数)に空間箱 B_n = [nℓ−ℓ/2, nℓ+ℓ/2]×[−ℓ/2,ℓ/2]²、
時刻 t_k = k T_g。サイト事象 G_{n,k}:「t_k に B_n 内に、殻速度のマーク生存粒子が存在」。

*(2) 1世代の中継補題*: G_{n,k} の親粒子 P₀ について(生存は t_k 時点で条件付け —
指数寿命の無記憶性より残余寿命は新鮮な Exp(T_s)):
- P₀ は [t_k, t_k+T_s] の間、自軌道カプセルに入射する殻粒子と遭遇する。
  入射は (x₀,v) 空間の Poisson 制限だから、
  「速度が錐 C_± = {w: ∠(w, ±e₁) ≤ θ} に入り、かつ伝播成功し、かつ
  マーク後 t_{k+1} まで生存し、かつ t_{k+1} に B_{n±1} に居る」子の数は
  **Poisson(μ_±)、μ_± ≥ c(θ, 幾何)·e^{−T_g/T_s}·p·ρq πR² w₁ q T_s**
  (θ と ℓ, T_g の整合で c > 0 を確保。子のマーク時刻の揺れ ≤ τ+T_s は
  箱幅 ℓ が吸収する)。
- ゆえに P(子が B_{n±1} に生存到達) ≥ 1 − exp(−μ) =: 1 − ε、
  μ ∝ p λ_shell T_s → ∞ で ε → 0。

*(3) 独立性の管理*:
- 同一セルの ± 両ボンドは**速度錐で標的クラスが排反**(1粒子の速度は1錐にのみ属す)。
- サイト事象は有界時空窓(箱+殻速度×T_g の掃引)内の Poisson 制限と
  親の残余寿命・ペアコインのみに依存 → **有限レンジ従属**の方向付きサイト場。
- Liggett–Schonmann–Stacey 優越: ε 十分小で独立 Bernoulli(1−δ) を優越 →
  超臨界方向付きパーコレーション → サイト鎖が無限に続く ⇒ 生存。∎(スケッチ)

*(4) 厳密化で埋める箇所(審査役向けの自己申告)*:
- (a) 中継補題の μ の下界の積分計算(錐×箱×時間窓の体積評価 — 機械的)
- (b) 「子の標的が親と同一粒子」等の退化事象の除去(Poisson の単純性で測度零/摂動)
- (c) LSS 適用のためのレンジ定数の明示
- (d) τ > T_s の場合の定数の追跡(T_g の取り方は既に τ 込み)

**数値反証との整合**: E1(疎領域の生存立ち上がり m≈1.0–1.3)・E3(運動救済)は
本定理の主張域(pλT_s 大)で生存を確認済み。定理の定数 C は非最適(数値は真の閾値が
分枝値 ≈1 近傍であることを示す)— 定理は存在と帰着、定量は数値、と役割分担。

## 層1の総括(セッション2時点)

**臨界面の存在(非自明な相転移)**:
- 下側(絶滅): m = p(deg + λT_s) < 1 ⇒ 絶滅(定理 A — 再マーク禁止版は完全証明)
- 上側(生存): p λ_shell T_s > C ⇒ 生存(定理 C2 — スケッチ完成)
- 両者は同じ無次元量 pλT_s の定数倍で挟む ⇒ **臨界値 (pλT_s)_c ∈ [max(0, 1−p·deg), C]
  が存在し有限非零** — 層1の中核言明。
- 静的極限(定理 B)ではこの描像がボンドパーコレーション制御に交代する(C5)。


## 規約二分法の完成(E7、セッション2)【状態: 数値確立・層3の中核言明】

装置 v2(進入時刻を CPA 近似 → 真の α に修正、hold 変種追加。既存テスト PASS、
E4c 回帰は数値完全再現)で滞在時間要求型(伝播に R 内滞在 hold が必要)を実装し対比:

| 規約 | σ 依存性 | 実験 |
|---|---|---|
| **進入駆動・撃ちっ放し**(交付規約 = CN19 探査機型) | **単調に助ける**(0.73→0.91 ほか) | E3, E4, E4b, E4c |
| **滞在時間要求型**(hold=0.8) | **非単調 — 最適速度 σ*≈0.4**(0.06→0.31→0.00) | E7 |

- 両端の死因が異なる: σ=0 は幾何(パーコレーション不可)、σ→∞ は滞在不足
- 物理文献との整合: Rodríguez+19 の最適速度 v*(滞在型に相当・数値)と同型。
  文献地図の「単調性は設定依存で一般定理なし」の実例を単一モデル内で構成
- **層3の最終形(目標)**: 「運動が入植を助けるか」の答えは伝播規約の関数である。
  撃ちっ放し型では単調(C4 予想+E4c)、滞在型では最適速度存在(E7)。
  前者の十分速い側は定理化済みの道筋(命題 D + C2)、後者の定理化は続編候補


---

# 審査サイクル1(交付文書 §3.3、2026-08-15)— 完了

審査役(文脈非共有の新規インスタンス)による定理A改訂版・定理C6a の行単位検証。

**判定**: 定理C6a =「軽微な修正で健全」(数学的誤りなし)。
定理A =「重大だが局所的・修復可能なギャップ、修正後に成立」— 審査役が修復を提示し
修正版の導出を検証済み(等号まで成立)。

## 定理 A(最終版 — 審査反映済み)【状態: **審査済み・証明確定**(再マーク禁止版)】

**主張**: ν は E|v|² < ∞ を満たすとする(等方・ガウスは不要)。
K(v,v′) = pρ(V_R + πR²|v−v′|T_s) を **L²(ν) 上の**積分作用素とみなし r(K) をスペクトル
半径とする。再マーク禁止版 M で r(K) < 1 ⇒ E[総マーク数] < ∞ ⇒ a.s. 絶滅。

**証明の骨子(審査役の修正を全採用)**:
1. マーク集合は世代帰納で構成され親子木をなす(整礎性)。
2. **決定的試行時刻の再帰**: t₀=0、t_j := max(inf T_{i_{j−1}i_j}, t_{j−1}+τ)。
   開路事象 =「各 j で T_j ∩ [t_{j−1}+τ, t_{j−1}+τ+L_{j−1}) ≠ ∅ ∧ コイン成功」。
   実マーク鎖の試行時刻はちょうどこの再帰を満たす(必要条件)。窓長はちょうど L_{j−1}、
   τ は窓の平行移動のみでカプセル体積に不寄与(→ 核に τ が現れない)。
   【初版の欠陥はここ: witness の入れ子存在量化で窓が接触継続時間分だけ膨らみ
   核に吸収不能だった — 審査指摘(α)】
3. Tonelli で内側から積分: x_j 積分 = カプセル体積 V_R + πR²|v_{j−1}−v_j|L_{j−1}
   (窓位置に不変)、L_{j−1} は当該ステップのみに出現し独立 → E[L]=T_s が入る。
   **E[#深さk開路] = ∫ν(dv₀)(K^k𝟙)(v₀)(等号)**。
4. K は対称非負核、E|v|²<∞ より Hilbert–Schmidt ⇒ L²(ν) 上有界自己共役、
   ⟨𝟙, K^k𝟙⟩ ≤ ‖K‖^k = r(K)^k。(Krein–Rutman 不要 — 審査指摘(iii))
5. Σ_k r(K)^k < ∞ ⇒ 総マーク数の期待値有限 ⇒ max(m_i+L_i) < ∞ ⇒ 絶滅。∎

## 定理 C6a(最終版 — 審査反映済み)【状態: **審査済み・証明確定**】

審査判定「数学的誤りなし」。3点の明示化を反映:
(1) v₀ 条件付けを明示(λ_W = λ_W(v₀)、無条件では mixed Poisson)。
(2) **等方性の消費点**: λ_W > 0 a.s. は「回転不変測度は原点外に原子を持たない」
    ことによる(ν_W({v₀}) = 0)— 等方性を落とすと原子的 ν で破綻しうる。
(3) 遭遇=試行の3点根拠(不死ゆえ全窓で試行 / 弾道ゆえペアごと遭遇高々1回 /
    ペアごと独立コイン)。o(t) は固定粒子ごとの定数で一様性不要(審査確認済み)。

## 検証文化の記録(セッション2終了時点)

- 自己審査: 定理A初版の相関エラーを検出(スカラー化不能)→ スペクトル条件へ改訂
- 審査役: 改訂版にもなお残っていた窓量化の欠陥(α)を検出し修復を提示・検証。
  「等号成立」「ガウス不要」の2つの改良ももたらした
- **審査サイクルが設計どおり機能した初例**(交付文書 §3.3)
